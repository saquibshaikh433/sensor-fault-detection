from setuptools import find_packages, setup # find packages find the root packages

setup(
    name = "sensor_package",  # name of the package 
    version = "0.0.1",          # version of ur package 
    author = "najmu saquib",
    author_email = "saquibshaikh433@gmail.com",
    packages = find_packages(),
    install_requires = [] 
)

